<h1>Lista de Academias</h1>

<% @gyms.each do |gym| %>
  <h3> <%= link_to gym.name, gym %> </h3>
  <ul>
    <li> <%= gym.address %> </li>
    <li> <%= gym.open_hour %> </li>
    <li> <%= gym.close_hour %> </li>
    <li> <%= gym.working_days %> </li>
  </ul>
<% end %>

<%= link_to 'Voltar', root_path %>
